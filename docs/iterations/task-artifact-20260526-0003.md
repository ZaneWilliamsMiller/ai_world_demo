# 活纸·每日迭代 | task-artifact 2026-05-26_0003

## 目标
对活纸项目进行有质量的改进迭代

## 改进方向
🐛 Bug修复 — 午夜强制休憩不尊重夜行NPC

## 发现过程
1. 拉取最新代码（已是最新）
2. 审查近期commit历史（20条），了解连续5轮NPC大脑升级
3. 逐模块检查：prompts → NPC数据 → economy → encounter → gossip → agent_brain → core → routes
4. 在 `core.py:update_npc_states_from_habits()` 中发现：子丑寅时（0-2）强制所有NPC `in_active = False`，完全忽略 `nocturnal=True` 属性
5. 夜行NPC（hei/jianfei/shuizu）的活跃时段本身跨午夜（18-29），被此逻辑覆盖

## 修复
在 `backend/systems/core.py` L465-467：
- 修复前：`if sh in (0, 1, 2): in_active = False`  → 全员强制休憩
- 修复后：`if sh in (0, 1, 2): if not habits.get("nocturnal", False): in_active = False`

## CI结果
6/6 全部通过（Health/Hello/Talk/State/Saves/Import）