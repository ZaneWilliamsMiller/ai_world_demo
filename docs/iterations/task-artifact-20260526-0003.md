# 迭代总结 2026-05-26 00:21

## 目标
对「活纸」项目进行一次有质量的改进迭代。

## 方向选择：💬 对话体验 — NPC 说话风格个性化

近期迭代集中在 NPC 大脑（记忆、情绪、反思）和 Bug 修复，对话体验是较少触及的方向。
分析代码后发现：16 位 NPC 中仅 8 位定义了「声口」，且 format_npc_character_sheet() 将其与其他角色特质混排，LLM 容易忽略。

## 改进内容

1. **npcs_data.py**：为 8 位缺失「声口」的 NPC 逐一撰写个性化说话风格
   - 掌柜、牙人、皂隶、镖头、黑店、船家、里正、知客
   - 每条描述包含：句式偏好、惯用语、语气特征、称呼习惯

2. **models/npc.py**：format_npc_character_sheet() 强化
   - 提取「声口」为 `★【说话风格——必须遵守】` 独立区块
   - 追加 `★ 每句台词、每个动作描写都应符合上述风格` 提示

3. **services/talk_service.py**：静态层注入风格铁律
   - 在 static_parts 中增加 `【风格铁律】` 指令（仅当角色含风格标记时）
   - 告诉 LLM「不可妥协的角色一致性要求」

## CI 结果
6/6 全部通过：Health ✅ | Hello ✅ | Talk (17.5s) ✅ | State ✅ | Saves ✅ | Import ✅

## 提交
a8a7b42 auto: NPC说话风格个性化——16位NPC全员配备「声口」指引 | NPC Speech Style Persona Enhancement
- GitHub push 因网络问题未完成，待下次重试