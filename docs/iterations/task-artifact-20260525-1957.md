# Task Artifact — raw_debug泄漏修复 + 情绪惯性升级

**时间**：2026-05-25 19:57 CST
**触发**：cron:3fc79465-e5ef-47d3-a3b0-b6653a275e37 活纸·每日迭代

## 决策过程

1. 执行了 git pull → 已是最新
2. 阅读了最近15个commit → 大量NPC大脑升级、bug修复
3. 运行全端点CI测试 → 6/6通过，发现 `raw_debug` 字段泄漏
4. 分析代码 → `talk_service.py` 的 `apply_npc_reply()` 返回值包含 `parsed.model_dump_json()`
5. 选择双目标改进：(a) 修复 raw_debug 泄漏 (b) 升级情绪惯性

## 实施内容

### Bug修复: raw_debug 信息泄漏
- 删除 `apply_npc_reply` 返回 dict 中的 `"raw_debug": parsed.model_dump_json()`
- 影响：API响应不再包含内部调试信息

### NPC大脑: 情绪惯性 (Mood Inertia)
- 在 `_evolve_npc_mood()` 中基于当前效价/唤醒度调制情绪变化速率
- 正性耐受、负性放大、唤醒度惯量三种机制
- 改善NPC情绪自然度

## CI结果
全部6项测试通过，无regression。

## 文件
- 迭代记录: `docs/iterations/2026-05-25_1957.md`
- 修改文件: `backend/services/talk_service.py` (+25/-1)