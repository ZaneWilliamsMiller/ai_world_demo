# Task Artifact — 2026-05-24_0802

## Objective
修复存档系统的 Memory 反序列化 Bug，解决所有存档加载静默失败的问题。

## Key Reasoning
1. `_deserialize_player()` 中 `Memory()` 构造传了 `anchor=` 而非 `is_anchor=` → TypeError
2. `Memory` 缺省必填字段 `id`/`created_at`/`last_accessed` → TypeError
3. `linked_memory_ids` 未序列化 → 顿悟防重链接失效
4. `Memory.to_dict()` 缺少 `created_at`/`last_accessed` → recency 衰减丢失

## Changes
- `backend/memory.py`: `to_dict()` 新增两时间戳字段
- `backend/systems/save_system.py`: `_mind_to_dict()` 新增 `linked_memory_ids`；`_deserialize_player()` 重写 Memory 构造
- `docs/iterations/2026-05-24_0802.md`: 迭代记录
- `docs/PROJECT_STRUCTURE.md`: 更新时间戳

## Verification
- 存档回环测试 3条记忆完整还原 ✅
- valence/arousal/linked_memory_ids/favor/reputation/npc_states 均一致 ✅
- 服务启动无报错 ✅
- `/api/hello` → 200 ✅